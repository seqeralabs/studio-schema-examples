# Studio schema examples

This repository branch shows how multiple environmental variables can be defined in a Git repository and used in a Studio.

It also includes a simple Python script to scrape some images from the internet. The libraries used in the script are pinned to specific versions in the `.seqera/environment.yaml` file.
